# URL pública do backend (Railway) para o WebSocket em produção.
# Deixe vazio em dev (o front usa a mesma origem / proxy do Vite).
# Aceita https://host, wss://host ou wss://host/ws (o /ws é acrescentado sozinho).
# Ex.: wss://satsmeter-backend.up.railway.app
VITE_WS_URL=
