// SatsMeter — credenciais e conexão do dispositivo.
// Copie para config.h e preencha (config.h é ignorado pelo git):
//
//   cp include/config.h.example include/config.h
#pragma once

// --- Wi-Fi ---
const char* WIFI_SSID     = "SUA_REDE";
const char* WIFI_PASSWORD = "SUA_SENHA";

// --- Broker MQTT (HiveMQ Cloud, TLS) ---
const char* MQTT_HOST      = "SEU-CLUSTER.s1.eu.hivemq.cloud";
const int   MQTT_PORT      = 8883;
const char* MQTT_USER      = "esp32-satsmeter";
const char* MQTT_PASSWORD  = "SUA_SENHA_MQTT";
const char* MQTT_CLIENT_ID = "satsmeter-device-01";
